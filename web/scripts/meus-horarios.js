const BASE_URL='https://goplay-dzlr.onrender.com';
const user=JSON.parse(localStorage.getItem('usuarioLogado')||'null');
const $=id=>document.getElementById(id); const esc=v=>String(v??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#039;');
async function api(url,opt={}){const r=await fetch(url,opt),t=await r.text().catch(()=>"");let d=null;try{d=t?JSON.parse(t):null}catch{}if(!r.ok)throw new Error(d?.error||t||`HTTP ${r.status}`);return d;}
function dte(v){return v?new Date(v).toLocaleDateString('pt-BR'):'—';}
function counts(p=[]){return {vou:p.filter(x=>x.status==='VOU').length,nao:p.filter(x=>x.status==='NAO_VOU').length,pend:p.filter(x=>x.status==='PENDENTE').length};}
function myStatus(p=[]){return p.find(x=>Number(x.usuarioId)===Number(user.id))?.status||'PENDENTE';}
async function load(){
  const [groups,u]=await Promise.all([api(`${BASE_URL}/grupos-horario/meus`),api(`${BASE_URL}/usuarios/${user.id}`)]);
  $('toggleDisponivel').checked=!!u.disponivelParaConvites;
  const box=$('gruposLista');
  if(!groups.length){box.innerHTML='<div class="p4-empty" style="grid-column:1/-1">Você ainda não participa de nenhum horário recorrente.<br>Crie um grupo ou aguarde um convite no 🔔 sininho.</div>';return;}
  box.innerHTML=groups.map(g=>{const a=g.proximo,c=a?counts(a.presencas):null,st=a?myStatus(a.presencas):null;const hf=g.horariosFixos?.[0];return `<article class="p4-card"><div class="p4-topline"><span class="p4-chip">${g.euOrganizo?'⭐ Organizador':'👥 Jogador'}</span><span class="p4-chip">${g.membros.length}/${g.maxJogadores}</span>${hf?`<span class="p4-chip ${hf.status==='APROVADO'?'yes':hf.status==='PENDENTE'?'wait':'no'}">${hf.status==='APROVADO'?'✓ Fixo':hf.status==='PENDENTE'?'⏳ Aguardando empresa':'✕ Recusado'}</span>`:''}</div><h3 style="margin-top:10px">${esc(g.nome)}</h3><div class="p4-muted">📍 ${esc(g.society?.nome||'Empresa')}</div>${a?`<div style="margin-top:14px"><strong>Próximo: ${dte(a.data)} • ${esc(a.horaInicio)}</strong><div class="p4-topline" style="margin-top:8px"><span class="p4-chip yes">👍 ${c.vou}</span><span class="p4-chip no">👎 ${c.nao}</span><span class="p4-chip wait">⏳ ${c.pend}</span></div><div style="margin-top:8px" class="p4-muted">Sua resposta: ${st==='VOU'?'👍 Vou':st==='NAO_VOU'?'👎 Não vou':'⏳ Pendente'}</div></div>`:'<div class="p4-muted" style="margin-top:14px">Nenhum encontro futuro criado ainda.</div>'}<div class="phase4-actions" style="margin-top:14px"><button class="p4-btn p4-dark" onclick="location.href='horario-grupo.html?grupoId=${g.id}'">Abrir grupo</button>${a?`<button class="p4-btn p4-primary" onclick="location.href='confirmar-presenca.html?agendamentoId=${a.id}'">Confirmar presença</button>`:''}</div></article>`}).join('');
}
async function loadCompanies(){if(window.GoPlayEmpresaContextReady)await window.GoPlayEmpresaContextReady;let list=window.GOPLAY_EMPRESAS||[];if(!list.length)list=await api(`${BASE_URL}/society`);$('grupoSocietyId').innerHTML='<option value="">Selecione</option>'+list.map(x=>`<option value="${x.id}">${esc(x.nome)}${x.cidade?' — '+esc(x.cidade):''}</option>`).join('');const cur=Number(localStorage.getItem('societyId')||0);if(cur)$('grupoSocietyId').value=String(cur);}
$('toggleDisponivel')?.addEventListener('change',async e=>{try{await api(`${BASE_URL}/jogadores/disponibilidade`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({disponivel:e.target.checked})});}catch(err){alert(err.message);e.target.checked=!e.target.checked;}});
$('btnNovoGrupo')?.addEventListener('click',async()=>{await loadCompanies();$('modalNovoGrupo').classList.add('show');});
$('fecharNovoGrupo')?.addEventListener('click',()=>$('modalNovoGrupo').classList.remove('show'));
$('modalNovoGrupo')?.addEventListener('click',e=>{if(e.target.id==='modalNovoGrupo')e.currentTarget.classList.remove('show');});
$('formNovoGrupo')?.addEventListener('submit',async e=>{e.preventDefault();try{const g=await api(`${BASE_URL}/grupos-horario`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({societyId:Number($('grupoSocietyId').value),nome:$('grupoNome').value,descricao:$('grupoDescricao').value,maxJogadores:Number($('grupoMax').value||20)})});location.href=`horario-grupo.html?grupoId=${g.id}`;}catch(err){alert(err.message);}});
document.addEventListener('DOMContentLoaded',()=>load().catch(e=>{$('gruposLista').innerHTML=`<div class="p4-empty">${esc(e.message)}</div>`;}));
